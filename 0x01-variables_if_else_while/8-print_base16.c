#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int i;
	int j;

	for (i = 0; i < 10; i++)
		putchar(i);
	for (j = 97; j < 103; j++)
		putchar(i);
	putchar('\n');

	return (0);
}
